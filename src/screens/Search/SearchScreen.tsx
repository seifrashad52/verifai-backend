import { View, Text } from "react-native";

// Hosts the "Check a Claim" tool: text input, image/screenshot picker,
// and the true/false/unverifiable verdict result card.
export default function SearchScreen() {
  return (
    <View>
      <Text>Check a Claim</Text>
    </View>
  );
}
